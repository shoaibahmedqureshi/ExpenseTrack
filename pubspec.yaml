name: outlay
description: "Outlay — track your expenses with ease."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+42

environment:
  sdk: ^3.5.3

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8
  provider: ^6.1.2
  sqflite: ^2.3.3+1
  path: ^1.9.0
  intl: ^0.19.0
  google_mlkit_text_recognition: ^0.16.0
  image_picker: ^1.1.2
  image: ^4.9.1
  # Native guided capture: ML Kit Document Scanner UI on Android, VisionKit
  # on iOS — live edge detection, auto-crop/perspective correction, flash.
  # Raw ungated camera photos were the receipt scanner's dominant failure
  # source (blur/framing/rotation), not the OCR or parsing stages.
  flutter_doc_scanner: ^0.0.21
  supabase_flutter: ^2.8.4
  connectivity_plus: ^6.1.4
  shared_preferences: ^2.3.4
  google_sign_in: ^6.2.2
  # Required by Apple Guideline 4.8 whenever a third-party sign-in (Google,
  # here) is offered — Sign in with Apple must be offered as an equivalent
  # option, or App Review rejects the submission outright.
  sign_in_with_apple: ^6.1.4
  crypto: ^3.0.3
  in_app_purchase: ^3.2.0
  fl_chart: ^0.69.2
  pdf: ^3.11.1
  path_provider: ^2.1.4
  share_plus: ^10.0.0
  url_launcher: ^6.3.1
  webview_flutter: ^4.10.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  sqflite_common_ffi: ^2.3.4

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.14.1
  flutter_native_splash: ^2.4.3

flutter_launcher_icons:
  android: true
  ios: true
  remove_alpha_ios: true
  image_path: "assets/branding/app_icon_1024.png"
  min_sdk_android: 21
  web:
    generate: true
    image_path: "assets/branding/app_icon_1024.png"
    background_color: "#0f766e"
    theme_color: "#0f766e"
  windows:
    generate: true
  macos:
    generate: true

# sqflite_common_ffi 2.3.5's Windows setup imports package:sqlite3/open.dart,
# which was removed/restructured in sqlite3 3.x even though its pubspec
# constraint (>=2.3.0 <4.0.0) doesn't reflect that break. Pinning to a 2.x
# release keeps sqflite_common_ffi (used only in tests, for an in-memory DB)
# actually compiling.
dependency_overrides:
  sqlite3: ^2.4.6

flutter_native_splash:
  color: "#ffffff"
  image: "assets/branding/splash_logo.png"
  color_dark: "#0f766e"
  image_dark: "assets/branding/splash_logo.png"
  android_12:
    color: "#ffffff"
    image: "assets/branding/app_icon_1024.png"
    icon_background_color: "#ffffff"
    color_dark: "#0f766e"
    image_dark: "assets/branding/app_icon_1024.png"
    icon_background_color_dark: "#0f766e"
  web: false

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    - assets/branding/

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # Bundled locally rather than fetched at runtime via google_fonts — the
  # runtime-fetch approach silently falls back to the platform default font
  # if the device/simulator can't reach fonts.gstatic.com at first launch,
  # with no error surfaced, which was masking whether Manrope was actually
  # applying.
  fonts:
    - family: Manrope
      fonts:
        - asset: assets/fonts/Manrope-Regular.ttf
          weight: 400
        - asset: assets/fonts/Manrope-Medium.ttf
          weight: 500
        - asset: assets/fonts/Manrope-SemiBold.ttf
          weight: 600
        - asset: assets/fonts/Manrope-Bold.ttf
          weight: 700
